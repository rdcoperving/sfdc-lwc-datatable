# sfdc-lwc-datatable(with flatten object)
Salesforce Lightning Web Component datatable with flatten object

Takes a nested Javascript object and flatten it

# Method:
flatten(object) -- Takes object as parameter

# Usage:
this.flatten(object));
